module Kicksite
  class HashObject
    def initialize(payload = {}, persisted = false)
      #payload.each do |key, value|
        #recursive_define(key, value)
      #end
      byebug
      puts 'hi'
    end
  end
end

